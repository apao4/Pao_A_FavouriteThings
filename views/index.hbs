<section id="plants">
    {{!-- <h1>My Favourite Things</h1> --}}
    {{#each avatars}}
        <div class="plant-container">
            <a href="/{{name}}">
                <img src="/images/{{avatar}}" alt="avatar">
            </a>

            <h3 class="plantName">{{name}}</h3>
        </div>
    {{/each}} <!-- this is a repeating section -->

</section>