const config = {
    user: "root",
    pword: "root", // windows users leave this blank
    host: "localhost",
    port: "8889", // windows users - sql port is 3306
    database: "db_marvelcharacters"
}

module.exports = config;